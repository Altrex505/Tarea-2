#include <stdio.h>
int main()
{
int N, i;
  printf("Ingrese el tamaño del arreglo unidimensional:\n");
scanf("%d", &N);
  int mat[N];
  for (i=0;i<N;i++)
    {
    printf("Ingrese el valor entero %d del arreglo:", i+1);
    scanf("%d", &mat[i]);
    }
  for (i=0;i<N;i++) 
    {
    
    } 
  printf("
} 
