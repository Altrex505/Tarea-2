#include <stdio.h>
int main()
{
int mat[N], N, i;
  printf("Ingrese el tamaño del arreglo unidimensional:\n");
scanf("%d", &N);
  for (i=0;i<N;i++)
    {
    printf("Ingrese el valor %d del arreglo:", i+1);
    scanf("%d", &mat[i]);
    
    } 
} 
